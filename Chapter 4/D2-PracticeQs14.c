#include<stdio.h>
// Q. Print no.s from 0 to n. (n should be entered by the user)
int main(){ 
     printf("enter a number: ");
     int n;
    scanf("%d",&n);
    for (int i= 0; i <=n; ++i){
        printf("%d \n", i);
    printf("%d",n );
    }
    return 0;
}